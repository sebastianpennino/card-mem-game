'use strict';

/**
 * declare a namespace to avoid global namespace pollution
 * making sure we're not overwriting it
 * 
 * @author Sebastian Pennino 2014
 * 
 */

var myMemGame = myMemGame || myMemGame || {};